# =============================================================================
# Job Application Preprocessor - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# =============================================================================
# Storage
# =============================================================================
# Job data is stored in a local SQLite database at ./local_data/jobs.db
# Use the Streamlit dashboard (streamlit run dashboard.py) to view and manage jobs.

# =============================================================================
# Apify Configuration
# =============================================================================
# Your Apify API token for LinkedIn job scraping
# Get it from: https://console.apify.com/account/integrations
APIFY_API_TOKEN=your_apify_token_here

# Optional: multiple Apify tokens (comma-separated) for automatic failover.
# When a token hits a rate limit / quota / monthly hard limit, the app rotates
# to the next one. Tokens listed here are tried before APIFY_API_TOKEN above.
# APIFY_API_TOKENS=token_one,token_two,token_three

# Skip all Apify calls (job search, JD/CO fallback). Use after a collection run to
# resume filtering/analysis without incurring more Apify usage.
# SKIP_APIFY_COLLECTION=true

# =============================================================================
# API Server Configuration
# =============================================================================
# URL of the API server for job analysis and resume/cover letter generation
SERVER_URL=https://your-api-server.com

# API key/secret for authenticating with the server
API_KEY=your_api_secret_key_here

# =============================================================================
# Google Gemini API Configuration
# =============================================================================
# Primary Gemini API key for LLM operations
# Get it from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Backup Gemini API key (optional, used if primary hits rate limits)
BACKUP_GEMINI_API_KEY=your_backup_gemini_api_key_here

# Optional: multiple Gemini API keys (comma-separated) for automatic failover.
# When a key hits a rate limit / quota / 429 / 503, the app rotates to the next
# one. Keys listed here are tried before GEMINI_API_KEY and BACKUP_GEMINI_API_KEY.
# GEMINI_API_KEYS=key_one,key_two,key_three

# Gemini model to use for analysis and generation
# Default: gemini-2.0-flash
GEMINI_MODEL=gemini-3.1-flash-lite

# Rate limit: requests per minute (RPM) and per day (RPD) for Gemini API.
# Free tier is typically 5 RPM / 25 RPD; use slightly lower (e.g. 4/20) to stay under.
# Increase for paid tier.
# GEMINI_RPM=4
# GEMINI_RPD=20

# =============================================================================
# Personalization
# =============================================================================
# Path to your resume in PDF format.
# If resume_data.json is missing, the app will try to generate it using this PDF.
RESUME_PDF_PATH=path/to/your/resume.pdf

# =============================================================================
# Feature Flags
# =============================================================================
# Enable sustainability checking for companies (set to "true" to enable)
# Default: false
CHECK_SUSTAINABILITY=false

# =============================================================================
# Telegram Bot (optional)
# =============================================================================
# Sends ready application packages (resume + cover letter) to your phone.
#
# Setup:
#   1. Message @BotFather on Telegram → /newbot → copy the bot token.
#   2. Set TELEGRAM_BOT_TOKEN below and restart the app.
#   3. Open your bot in Telegram and send /start.
#   4. The bot replies with your numeric chat ID — set TELEGRAM_CHAT_ID to that
#      value (your user ID, not the bot's). Also saved to local_data/telegram_chat_id.txt.
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Your Telegram user chat ID (optional if /start was already sent while the app was running).
# TELEGRAM_CHAT_ID=123456789

